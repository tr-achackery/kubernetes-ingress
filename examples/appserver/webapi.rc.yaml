apiVersion: v1
kind: ReplicationController
metadata:
  name: webapi-rc
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: webapi
    spec:
      containers:
      - name: webapi
        image: achackery/webapi-asi:1.0.0
        ports:
        - containerPort: 80
